export interface IPosition {
  x: number;
  y: number;
}

export interface ISize {
  width: number;
  height: number;
}

// ? needed?
export interface IRotation {
  x: number;
  y: number;
  z: number;
}
